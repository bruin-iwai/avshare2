{
  "name": "avshare2",
  "version": "1.0.0",
  "description": "My sensational Nuxt.js project",
  "author": "Katsuhito Iwai",
  "private": true,
  "scripts": {
    "clean": "nuxi cleanup",
    "dev": "nuxi dev",
    "build": "nuxi build",
    "start": "nuxi preview",
    "generate": "nuxi generate",
    "prettier:check": "prettier --check --ignore-path .gitignore *.{js,ts,vue} **/*.{js,ts,vue}",
    "prettier:write": "prettier --write --ignore-path .gitignore *.{js,ts,vue} **/*.{js,ts,vue}",
    "lint": "eslint --ext .js,.ts,.vue --ignore-path .gitignore .",
    "prepare": "husky install"
  },
  "dependencies": {
    "vuetify": "^3.1.11"
  },
  "devDependencies": {
    "@nuxtjs/eslint-config-typescript": "^12.0.0",
    "@nuxtjs/eslint-module": "^4.0.2",
    "eslint": "^8.36.0",
    "eslint-config-prettier": "^8.8.0",
    "eslint-plugin-nuxt": "^4.0.0",
    "husky": "^8.0.3",
    "lint-staged": "^13.2.0",
    "nuxt": "^3.3.2",
    "prettier": "^2.8.7",
    "sass": "^1.60.0"
  },
  "lint-staged": {
    "*.{js,ts,vue}": "eslint --cache --fix",
    "*.{js,ts,css,md,vue}": "prettier --write"
  }
}
